import { CooperateIcon } from '../icons/CooperateIcon'
import { Tool } from '../interface'

export const SETTINGS = {
  siteName: 'DevBoost'
}
export const ListPageDocs: Tool[] = [
  { slug: 'how-to-contribute', name: 'How to contribute?', iconEl: CooperateIcon({}) }
]
